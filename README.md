  git clone https://github.com/roseyangyu/relax.git

  sudo cp command-not-found.py /etc/
  
  sudo vim /etc/bash.bashrc
  
  add "python command-not-found.py" in front of  "if [ -x /usr/lib/command-not-found ];"
